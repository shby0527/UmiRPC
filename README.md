# High Performance RPC Framework on .NET

### Work In Progress ###

server implement interface and provider this interface assembly to client, client will auto proxy and transfer the local invoke to the remote call.

server side, the method call will not based on reflection

- client interface auto proxy (OK)
- server method invoke cache (OK)
- RPC protocol design (Doing)
- RPC implement

waiting for our works